<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Article List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #e5ddd5; /* WhatsApp light background */
            color: #075e54; /* WhatsApp dark green */
            margin: 0;
            padding: 20px;
        }

        h2 {
            color: #128c7e; /* WhatsApp lighter green */
        }

        .article-container {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
            margin-bottom: 20px;
        }

        .article-content {
            margin-bottom: 20px;
        }

        .article-actions {
            margin-top: 10px;
        }

        .article-actions a {
            text-decoration: none;
            color: #ffffff;
            background-color: #075e54; 
            padding: 8px 16px;
            border-radius: 4px;
            margin-right: 10px;
            font-size: 14px;
        }

        .article-actions a:hover {
            background-color: #128c7e;
        }
    </style>
</head>
<body>
    <%- include("navbar") %>
    <h1>Article</h1>

    <% articaldata.forEach((el, i) => { %>
        <div class="article-container">
            <h2><%= el.title %></h2>
            <p class="article-content"><%= el.content %></p>
            <div class="article-actions">
                <% if (user && user.role === 'admin') { %>
                   
                    <a href="/Articles/delete/<%= el._id %>" onclick="return confirm('Are you sure you want to delete this article?');">Delete</a>
                    <!-- <a href="/Articles/delete/<%= el._id %>">Delete</a> -->
                <% } %>
            </div>
        </div>
    <% }) %>
</body>
</html>
